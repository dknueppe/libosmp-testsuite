include:
  template: SAST.gitlab-ci.yml

stages:
    - build
    - test
    
build:
    stage: build
    script:
        - make -j$(nproc --all)
    artifacts:
        paths:
            - build

testsuite:
    stage: test
    script:
        - python3 testsuite/testit.py
    allow_failure: true